var classy = require('classy');
var index = classy.define({
    forceInstance: true,
    init: function (ctx) {
        this.array = require('./array')();
        this.token = require('./token');
        this.web = require('./web')();
        this.api = require('./api')();
    }
});

classy.override(index, require('./core'))

module.exports = index;