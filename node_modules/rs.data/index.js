/**
* @namespace rs.data
* @author Santosh Shrestha
*/
var index = {
    model : require('./src/model'),
    db : require('./src/db'),
    client : require('./src/client'),
    addValidation: function (v) {
        
    },
    addDatabase: function (d) {
        
    }
};

module.exports = index;